# Optional dependencies for tree-sitter language support
# Install these if you need specific language parsing
# Note: Some may not have Python 3.13 wheels yet - install from source if needed

tree-sitter-python>=0.25.0  # Python grammar
tree-sitter-javascript>=0.25.0  # JavaScript grammar  
tree-sitter-typescript>=0.25.0  # TypeScript grammar

# To install from source if wheels aren't available:
# git clone https://github.com/tree-sitter/tree-sitter-python
# cd tree-sitter-python
# pip install .
